Fusion = {
    ['Main'] = {
        Locations = {
            {coords = vec3(1,1,1), radius = 5}
        },
        Items = {
            {price = 5, type = 'item', item = 'medkit', amount = 1 }, -- if no amount then defaults to 1
            {type = 'weapon', item = 'WEAPON_APPISTOL'} -- IF USING OX INVENTORY MAKE SURE TYPE IS ITEM
        },
        Teleports = {
            {label = 'Skate', coords = vec3(1,1,1)}
        }
    }
}